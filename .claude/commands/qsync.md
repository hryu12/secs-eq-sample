현재 작업을 안전하게 체크한 뒤 main 브랜치 최신 소스를 받아온다.

절차:
1. `git status`로 미커밋 변경사항이 있는지 확인한다. 있으면 사용자에게 알리고 중단한다.
2. 이상 없으면 `git checkout main`으로 이동한다.
3. `git pull origin main`으로 최신 소스를 받아온다.
4. 완료 후 현재 브랜치 상태를 알린다.
